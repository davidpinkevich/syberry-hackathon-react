import React from 'react';
import { useAppDispatch } from '../../hooks';
// import { getFavorites } from '../../redux/slices/sliceFavorites';

const Favorites = () => {
  
  return (
    <div onClick={useAppDispatch()}>Icon

    </div>
  )
}

export default Favorites