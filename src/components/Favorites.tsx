import { Link } from "react-router-dom"

const Favorites = () => {
    return (
        <div>
            <Link to={'/favorites'}>Избранное (12)</Link>
        </div>
    )
}

export default Favorites